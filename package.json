{
  "name": "notevaultpro-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install": "npm install --prefix server",
    "build": "npm install --prefix server",
    "start": "npm start --prefix server"
  }
}